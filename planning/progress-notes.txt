ORION FORGE — PROGRESS NOTES
============================
A running log of changes. Newest entries on top.

--------------------------------------------------
2026-06-26
--------------------------------------------------
ACCESS: Unlocked all agents, tools, and skins for everyone (pay-per-use, no paywall).
  - stripe_billing.py: get_user_unlocked_agents / user_has_tool_access /
    user_owns_item / get_user_purchases now grant the full catalog.
  - Reversible — full re-gating instructions in planning/access-gating.txt.

UI: Removed free-trial messaging (web/templates/plans.html) — access no longer
  depends on the trial. Hid the Store "Tools" tab (now free); see
  planning/store-hidden-sections.txt.

AGENTS: Deleted the Astra Noctis and Madara agents entirely (profiles, prompts,
  directives, settings, avatars, store entries, manifest). Reordered the agent
  list (profiles + chat) via PINNED_AGENT_ORDER: codex_animus, k_os, elysia,
  orion, marcus, aristotle, janus, dalvarr, lux_umbra, then the rest. Added a
  "New Agent" card to the profiles grid (right after Codex Animus); the chat
  model select keeps "LLM Only" at the top and has no New Agent card.

ELYSIA: New personality prompt, soul script (re-headed into ## sections so it's
  indexed), and a new avatar. Default agent avatars are now tracked in git so
  fresh deploys ship with them.

--------------------------------------------------
2026-06-24
--------------------------------------------------
ADMIN ACCESS: Made the Admin nav tab reveal off the authoritative backend check.
  - /api/auth/user now returns "is_admin" (from _check_admin); base.html reveals the
    Admin tab when is_admin is true (kept owner email/UID as fallback).
  - Backend already grants admin to UID a2cddfbd-dabb-4d50-b515-b600a08ce8e8 via the
    ADMIN_USER_IDS default; Fly has ADMIN_EMAILS set but NO ADMIN_USER_IDS secret.
  - NOT yet deployed — needs: fly deploy -a orionforge-engine.
  - If still no access after deploy, confirm the account's real Supabase user id
    (it may differ from a2cddfbd) and add it to ADMIN_USER_IDS.

STORE: Hid the Packs / Agents / Skins tabs for everyone (they are all provided free).
  - Edited orion-ui-standalone/web/templates/store.html and ui/web/templates/store.html.
  - Tab buttons get inline style="display:none;" (reversible; sections kept in code).
  - Full details + how to re-enable: planning/store-hidden-sections.txt
  - NOT yet deployed — needs: fly deploy -a orionforge-engine

BILLING / PAYMENTS (shipped earlier today, commit b82702d):
  - Stripe credit checkout fixed for test mode (live product id only under sk_live_).
  - Verified Stripe webhook events normalized via Event.to_dict() so signed
    credit-pack webhooks grant credits.
  - AnthropicClient now honors a user's own profile api_key (bring-your-own key).
  - Confirmed LIVE: Stripe publishable key is pk_live_ and configured=true.
  - Confirmed LIVE: platform OpenRouter passthrough deducts credits at 2x token cost
    (platform_hosted connection seeded from OPENROUTER_API_KEY; both chat paths deduct).

TESTS:
  - Full torture suite: 3389 passed, 0 failed.
